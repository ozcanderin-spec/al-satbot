name: Position Monitor (Disabled)

on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled - Supabase owns position closing
        run: echo "Position closing is owned exclusively by Supabase run_virtual_sell_engine() via pg_cron."
